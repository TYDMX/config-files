{
  "name": "sub-store-server",
  "private": true,
  "description": "Sub-Store backend for subscription caching",
  "scripts": {
    "start": "node -e \"process.env.SUB_STORE_BACKEND_API_PORT=process.env.PORT||3000;require('./sub-store.bundle.js')\""
  },
  "engines": {
    "node": ">=18"
  }
}
